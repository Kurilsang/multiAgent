# ReAct 循环用显式状态机编排,而非责任链

四节点循环(思考→行动→观察→判断)存在回边——判断"继续"就回到思考——且未来要支持跳步、并行与多 Agent 协作。我们决定把节点实现为状态、迁移表做成数据:回边只是一条边,多 Agent 阶段通过新增状态与迁移边扩展,引擎不变。责任链方案被否决:它只能表达线性传递(handler 可截断),回边要靠外层循环打补丁,将来表达多 Agent 拓扑时需要整体重写。
