# 任务终止采用双层判定

模型侧通过调用 finish 工具宣告语义层面的完成;引擎侧强制迭代硬上限(默认 8,可配置)与死循环止损(连续 3 次同工具同参数即 FAILED),预算耗尽以 partial 终态返回轨迹摘要。独立的 Critic LLM 反思调用被否决:每轮成本翻倍,且它本质上是第二个 Agent——留给多 Agent 阶段再引入。代价是语义判断完全依赖模型自身,只靠硬上限兜底。
