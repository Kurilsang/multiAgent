openai>=1.40
pydantic-settings>=2.3
fastapi>=0.110
uvicorn>=0.29
rich>=13.7
httpx>=0.27          # FastAPI TestClient（测试用）
mcp>=2.2             # MCP 客户端（stdio 会话）与真协议测试锚点
httpx2>=2.5          # mcp SDK 的 HTTP 传输（streamable-http 会话直接 import，显式声明防传递依赖漂移）
